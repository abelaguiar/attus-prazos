server {
    listen 80;

    location / {
        root /usr/share/nginx/html;
        index index.html;
        # SPA: qualquer rota desconhecida cai no index.html (roteamento no cliente)
        try_files $uri $uri/ /index.html;
    }
}
